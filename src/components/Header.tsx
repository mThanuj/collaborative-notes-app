import ProfileLogoutButtons from './ProfileLogoutButtons';

const Header = () => {
  return (
    <div className='flex max-w-xs justify-around gap-2'>
      <ProfileLogoutButtons />
    </div>
  );
};

export default Header;
